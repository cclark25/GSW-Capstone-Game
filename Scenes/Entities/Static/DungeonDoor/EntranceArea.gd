extends Area2D

func _ready():
	
	#connect("body_entered", self, "unlock")
		
	pass
	
#func body_entered(body):
	#if body.name == "player" && body.get("keys") > 0:
		#body.keys -= 1
		#set_process(false)
		#queue_free()	